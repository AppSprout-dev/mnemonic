# Advisory Board Review

When making significant decisions — architecture choices, experiment design, planning multi-step work, choosing between approaches, or deciding whether to ship or keep iterating — invoke the `advisory-board` skill.

Also invoke when GPU time or DO droplet credits are at stake.
